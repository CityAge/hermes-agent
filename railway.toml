[build]
    builder = "DOCKERFILE"
    dockerfilePath = "Dockerfile"

[deploy]
    startCommand = "rm -f /opt/data/.env && /opt/hermes/docker/entrypoint.sh gateway"
    restartPolicyType = "ON_FAILURE"
    restartPolicyMaxRetries = 10
