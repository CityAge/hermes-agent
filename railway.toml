startCommand = "mkdir -p /opt/data && echo \"OPENROUTER_API_KEY=$OPENROUTER_API_KEY\" >> /opt/data/.env && echo \"TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN\" >> /opt/data/.env && echo \"TELEGRAM_ALLOWED_USERS=$TELEGRAM_ALLOWED_USERS\" >> /opt/data/.env && /opt/hermes/docker/entrypoint.sh gateway 2>&1 || echo \"HERMES CRASHED WITH EXIT CODE $?\""
