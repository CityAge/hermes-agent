startCommand = "mkdir -p /root/.hermes && echo \"OPENROUTER_API_KEY=$OPENROUTER_API_KEY\" > /root/.hermes/.env && echo \"TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN\" >> /root/.hermes/.env && echo \"TELEGRAM_ALLOWED_USERS=$TELEGRAM_ALLOWED_USERS\" >> /root/.hermes/.env && /opt/hermes/docker/entrypoint.sh gateway"
